export enum Entrypoint {
  // ERC-20
  NAME = 'name',
  SYMBOL = 'symbol',
  APPROVE = 'approve',
  TRANSFER = 'transfer',

  // Escrow
  DEPOSIT = 'deposit',
  CLAIM = 'claim',
  GET_DEPOSIT = 'get_deposit',
}
